<Page x:Class="Practica.AddPages.AddBudgetCategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Practica.AddPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddBudgetCategoriesPage">
    <Page.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation
                        Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.5"  
                        />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>
    <Grid>


        <StackPanel   Margin="10,0,0,0" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>

                </Grid.RowDefinitions>

                <TextBlock Text="Имя категории" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <TextBox Text="{Binding CategoryName}" Margin="10" Grid.Column="1" Grid.Row="0"/>

                <TextBlock Text="Тип категории" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <TextBox Text="{Binding CategoryType}" Margin="10" Grid.Column="1" Grid.Row="1"/>

                <TextBlock Text="Описание" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <TextBox Text="{Binding Description}" Margin="10" Grid.Column="1" Grid.Row="2"/>
                <Button x:Name="SaveBtn" Grid.Column="1" Grid.Row="3" Margin="200,10,200,5" Width="150" Height="30" HorizontalAlignment="Center"  Content="Сохранить" Background="#67BA80" Click="SaveBtn_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="backBtn" Grid.Column="1" Grid.Row="4" Margin="200,10,200,5" Width="150" Height="30"   HorizontalAlignment="Center"  Content="Назад" Background="#67BA80" Click="backBtn_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>



        </StackPanel>
    </Grid>
</Page>
