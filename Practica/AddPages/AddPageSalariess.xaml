<Page x:Class="Practica.AddPages.AddPageSalariess"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Practica.AddPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddPageSalariess" Background="White">
    <Page.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation
                        Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.5"  
                        />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>
    <Grid>
        <StackPanel Margin="10,0,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Имя работника" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <ComboBox x:Name="Comb" SelectedItem="{Binding FirstName}" DisplayMemberPath="FirstName" Margin="10" Grid.Column="1" Grid.Row="0"  />

                <TextBlock Text="Финансовый год" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <ComboBox x:Name="comb2" SelectedItem="{Binding YearStart}" DisplayMemberPath="YearStart" Margin="10" Grid.Column="1" Grid.Row="1"/>

                <TextBlock Text="Месяц" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <TextBox Text="{Binding Month}" Margin="10" Grid.Column="1" Grid.Row="2" />

                <TextBlock Text="Начисление ЗП" Grid.Column="0" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <TextBox Text="{Binding Amount}" Margin="10" Grid.Column="1" Grid.Row="3"/>



                <TextBlock Text="Дата оплаты" Grid.Column="0" Grid.Row="4" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <TextBox Text="{Binding PaymentDate}" Margin="10" Grid.Column="1" Grid.Row="4"/>


                <Button x:Name="SaveBtn" Grid.Column="1" Grid.Row="5" Margin="200,10,200,10" Height="30" Width="150" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Сохранить" Background="#67BA80" Click="SaveBtn_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="backBtn" Grid.Column="1" Grid.Row="6" Margin="200,10,200,10" Height="30" Width="150" HorizontalAlignment="Center" VerticalAlignment="Center"  Content="Назад" Background="#67BA80" Click="backBtn_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

            </Grid>
        </StackPanel>
        
    </Grid>
</Page>
